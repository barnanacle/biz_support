# Claude Code Instructions

Claude Code and Codex share this repository. Follow `AGENTS.md` before making changes.

Key reminders:

- Work from `/Users/ryu/Antigravity/biz_support/biz_support_web`.
- Run `scripts/agent-sync.sh` before editing.
- Do not overwrite uncommitted local changes from another tool.
- Treat `data.json` as scheduled crawler output unless the user explicitly asks to edit data.
- Commit and push focused changes before handing work to another tool.
